pluginManagement {
    includeBuild("../node_modules/@react-native/gradle-plugin")
}

plugins {
    id("com.facebook.react.settings")
}

def nodeExecutable = "/Users/jeewanjamnal/.nvm/versions/node/v23.6.0/bin/node"
def reactNativeCli = file("../node_modules/react-native/cli.js").absolutePath

extensions.configure(com.facebook.react.ReactSettingsExtension) { ex ->
    ex.autolinkLibrariesFromCommand([
            nodeExecutable,
            reactNativeCli,
            "config"
    ])
}

rootProject.name = 'TestNativeModules'

include ':app'

includeBuild('../node_modules/@react-native/gradle-plugin')